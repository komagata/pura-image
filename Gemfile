source "https://rubygems.org"

gemspec

# Local development override — remove once pura-processing 0.1.0 is published to RubyGems.
gem "pura-processing", path: "../../../../pura-processing"

gem "rubocop", require: false
